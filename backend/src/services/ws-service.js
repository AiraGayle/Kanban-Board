// WS Service — STUB
// TODO (Person 4): Implement WebSocket server and broadcastToUser().
// Signature expected by server.js:  initWebSocketServer(httpServer)
// Signature expected by task-routes.js:  broadcastToUser(userId, payload)

function initWebSocketServer(_server) {
    console.log('WS Service: stub — Person 4 will implement this');
}

function broadcastToUser(_userId, _payload) {
    // no-op until Person 4 wires in the real WebSocket server
}

module.exports = { initWebSocketServer, broadcastToUser };
